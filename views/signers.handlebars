{{>header}}
<div class="signer-title">
    <h2>We the undersigned demand an asparagus emoji to fill the void we feel within us whenever we write text messages.</h2>
</div>
<div class="">
    <p id="map-text">Map of Signers:</p>
    <div id="signerMap"></div>
</div>
<div class="signer-container">
    {{#each signers}}
        {{#if this.url}}
            <div class=info-box>
                <a class="signers" href="//{{this.url}}">{{this.first}} {{this.last}}</a><p class="signers">{{>age-city}}</p>
            </div>
        {{else}}
            <div class=info-box>
                <p class="signers">{{this.first}} {{this.last}}</p><p class="signers">{{>age-city}}</p>
            </div>
        {{/if}}
    {{/each}}
</div>
<a class="button" id="keep" href="javascript:history.back()">Go Back</a>
